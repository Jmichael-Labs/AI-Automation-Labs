# AI Education System - Production Ready
name: AI Education System - 4 Telegram Channel Automation

on:
  workflow_dispatch:
  push:

jobs:
  emergency-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
    - name: 🚨 Emergency Fresh Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        clean: true
        
    - name: ✅ Verify Latest Commit
      run: |
        echo "📊 Current commit: $(git rev-parse --short HEAD)"
        echo "📊 Latest commits:"
        git log --oneline -3
        echo "✅ Using latest code from main branch"
        
    - name: 🧨 Nuclear Cache Clear
      run: |
        echo "💣 Clearing ALL cache locations..."
        rm -rf ~/.cache ~/.npm ~/.yarn ~/.cargo ~/.pip
        echo "🗑️ Cache annihilated"
        
    - name: 🐍 Setup Python (No Cache)
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: 🎬 Install Video Processing Tools
      run: |
        echo "🎬 Installing ffmpeg for video concatenation..."
        sudo apt-get update
        sudo apt-get install -y ffmpeg
        ffmpeg -version
        echo "✅ ffmpeg installed successfully"
        
    - name: 📦 Install Dependencies (Complete)
      run: |
        echo "📦 Installing complete dependencies for Vertex AI + Video..."
        python -m pip install --upgrade pip
        
        # Core dependencies
        pip install Flask==3.0.0 requests==2.32.4 python-telegram-bot==22.3
        pip install praw==7.8.1 beautifulsoup4==4.12.2
        
        # Google AI for video generation (COMPLETE VERTEX AI SUITE)
        pip install google-genai==1.29.0 google-generativeai==0.8.5
        pip install google-cloud-aiplatform google-cloud-storage
        pip install vertexai
        
        # Image processing
        pip install Pillow==11.3.0
        
        echo "✅ Complete install with Vertex AI support"
        
    - name: ☁️ Configure Google Cloud
      env:
        GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CLOUD_CREDENTIALS }}
      run: |
        echo "🔧 Setting up Google Cloud credentials for youtube-pro-469213..."
        echo "$GOOGLE_APPLICATION_CREDENTIALS_JSON" > /tmp/gcp-key.json
        export GOOGLE_APPLICATION_CREDENTIALS=/tmp/gcp-key.json
        echo "GOOGLE_APPLICATION_CREDENTIALS=/tmp/gcp-key.json" >> $GITHUB_ENV
        
        # Install gcloud CLI for authentication
        echo "📦 Installing gcloud CLI..."
        curl https://sdk.cloud.google.com | bash
        source ~/google-cloud-sdk/path.bash.inc
        
        # Authenticate using service account
        gcloud auth activate-service-account --key-file=/tmp/gcp-key.json
        gcloud config set project youtube-pro-469213
        
        echo "✅ Google Cloud configured for project: youtube-pro-469213"
        
    - name: 🎯 EMERGENCY AI EDUCATION SYSTEM
      env:
        # === TELEGRAM TOKENS ===
        TELEGRAM_LEGAL_TOKEN: ${{ secrets.TELEGRAM_LEGAL_TOKEN }}
        TELEGRAM_MEDICAL_TOKEN: ${{ secrets.TELEGRAM_MEDICAL_TOKEN }}
        TELEGRAM_SENIOR_TOKEN: ${{ secrets.TELEGRAM_SENIOR_TOKEN }}
        TELEGRAM_GENERAL_TOKEN: ${{ secrets.TELEGRAM_GENERAL_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        
        # === GUMROAD ===
        GUMROAD_API_KEY: ${{ secrets.GUMROAD_API_KEY }}
        GUMROAD_LEGAL_ID: ${{ secrets.GUMROAD_LEGAL_ID }}
        GUMROAD_MEDICAL_ID: ${{ secrets.GUMROAD_MEDICAL_ID }}
        GUMROAD_SENIOR_ID: ${{ secrets.GUMROAD_SENIOR_ID }}
        GUMROAD_GENERAL_ID: ${{ secrets.GUMROAD_GENERAL_ID }}
        
        # === KO-FI === (REMOVED - API doesn't support posting content)
        
        # === GOOGLE CLOUD ===
        GOOGLE_APPLICATION_CREDENTIALS: /tmp/gcp-key.json
        GOOGLE_PROJECT_ID: youtube-pro-469213
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        
        # === CONTACT ===
        EMAIL_CONTACT: ${{ secrets.EMAIL_CONTACT }}
        INSTAGRAM_CONSULTING: ${{ secrets.INSTAGRAM_CONSULTING }}
        
      run: |
        echo "🚨 EMERGENCY EXECUTION WITH CORRECT COMMIT"
        echo "📅 Time: $(date)"
        echo "📊 Commit: $(git rev-parse --short HEAD)"
        echo "🔍 Files in directory:"
        ls -la multi_platform_engine.py
        
        echo "🔍 Checking channel configuration:"
        grep -A 5 -B 5 "@AIEducationHub_bot" multi_platform_engine.py || echo "❌ Channel not found in code!"
        
        echo "🚀 EXECUTING WITH FIXED CHANNELS..."
        python multi_platform_engine.py
        
    - name: 🧹 Emergency Cleanup
      if: always()
      run: |
        rm -f /tmp/gcp-key.json
        echo "🧹 Emergency cleanup done"